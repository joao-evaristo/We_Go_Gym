<%= form_with(model: user_enrollment) do |form| %>
  <% if user_enrollment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_enrollment.errors.count, "error") %> prohibited this user_enrollment from being saved:</h2>

      <ul>
        <% user_enrollment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div style="color: white">
    <%= form.label :active, style: "display: block" %>
    <%= form.check_box :active %>
  </div>

  <div style="color: white">
    <%= form.label :nome_da_academia, style: "display: block" %>
    <%= form.text_field :academium_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

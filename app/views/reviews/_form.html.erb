<br>
<br>
<br>

<%= form_for([@academium, @academium.reviews.build]) do |form|%>
  <!-- simple_form_for([@academium, @academium.reviews.build])-->
  <%= form.number_field :rating, max: 5, min: 1%>
  <%= form.text_field :comment %>
  <%= form.submit :submit %>
<% end %>